services:
  build:
    build: .
    user: "${DOCKER_UID:-1000}:${DOCKER_GID:-1000}"
    volumes:
      - ..:/src:ro
      - ../build:/output
      - pip-cache:/tmp/pip-cache
    environment:
      - PIP_CACHE_DIR=/tmp/pip-cache

volumes:
  pip-cache:
