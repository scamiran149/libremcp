name: tunnel.ngrok
title: Ngrok tunnel provider
requires: [tunnel_manager]
config_inline: true
#config_disposition: tab

config:
  authtoken:
    type: string
    default: ""
    widget: password
    label: Auth Token
    helper: "Your ngrok authentication token"
