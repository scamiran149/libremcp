name: tunnel.bore
title: Bore tunnel provider
requires: [tunnel_manager]
config_inline: true
#config_disposition: tab

config:
  server:
    type: string
    default: bore.pub
    widget: text
    label: Bore Server
    helper: "The bore server address (default: bore.pub)"
