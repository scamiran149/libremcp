name: mcp
title: MCP protocol endpoint
requires: [document, config, events, http_routes]
config_inline: main

config:
  enabled:
    type: boolean
    default: true
    widget: checkbox
    label: Enable MCP Protocol
    public: true
