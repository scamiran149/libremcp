name: writer
title: Writer document tools
requires: [document, config]
config_inline: doc

config:
  max_content_chars:
    type: int
    default: 50000
    min: 1000
    max: 500000
    widget: number
    label: Max Content Size
    public: true
